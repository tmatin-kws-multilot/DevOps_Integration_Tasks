pipeline {
    agent any

    environment {
        NAME = 'TTamzidul'
        LASTNAME = 'Matin'
    }

    stages {
        stage('Build') {
            steps {
                sh 'echo $NAME $LASTNAME'
            }
        }
    }
}
